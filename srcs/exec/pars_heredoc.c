#include "../../includes/minishell.h"

char *heredoc_found(char *limiter, int i)
{
	(void)limiter;
	(void)i;
	// char *name_file; //le nom du fichier qui va etre stocké dans la liste chainee d'infile

	// name_file = malloc(sizeof(char) * ft_strlen(ft_itoa(i)));
	// if (!name_file)
	// 	return (NULL);
	// name_file = ft_strdup("/tmp/tmp.txt")
	// fd_hd = open()
	return NULL;

}